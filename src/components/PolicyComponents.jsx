export const Section = ({ title, children }) => (
  <div>
    <h3 className="text-lg md:text-xl font-semibold text-axiom-red mb-2">
      {title}
    </h3>
    <p>{children}</p>
  </div>
);

export const Section2 = ({ title, children }) => (
  <div>
    <h3 className="text-lg md:text-xl font-semibold text-axiom-red mb-2">
      {title}
    </h3>
    <p>{children}</p>
  </div>
);

export const Section3 = ({ title, children }) => (
  <div>
    <h3 className="text-lg md:text-xl font-semibold text-axiom-red mb-2">
      {title}
    </h3>
    <p>{children}</p>
  </div>
);
